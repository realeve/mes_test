{
    "scripts": {
        "test": "jest --coverage --coverageReporters=text-lcov | coveralls"
    },
    "dependencies": {
        "axios": "^0.18.0",
        "soap": "^0.25.0",
        "jest": "^23.6.0",
        "x2js": "^3.2.3"
    }
}